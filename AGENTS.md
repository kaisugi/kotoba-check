このリポジトリで、日本語の推敲支援ツール「ことば点検」を開発してください。
公開予定URLは https://kotoba.kaisugi.me/ です。

まず以下をAGENTS.mdに保存し、実装計画を作成してください。
その後、Linderaの技術検証まで実装してください。

目的:
- AIが書いた日本語に出やすい単語や言い回しを指摘する。
- 指摘箇所と理由を示し、利用者の推敲を助ける。
- AIが書いたかどうかの確率や、根拠のない総合スコアは表示しない。

参考:
- https://blog.p1ass.com/posts/textlint-rule-preset-ai-words-ja/
- https://github.com/p1ass/textlint-rule-preset-ai-words-ja
- https://sundryst.com/convenienttool/strcount.html

構成:
- Vite + TypeScript + HTML/CSSの静的サイト。
- 形態素解析はLindera WASM + IPADICを第一候補にする。
- 現行の公式リポジトリ、npm配布版、型定義を確認してから実装する。
- WASM初期化と解析はWeb Workerで行う。初期化は使い回す。
- textlint本体への依存は必須にしない。
- 解析結果をsurface、lemma、pos、start、endの共通形式に変換する。
- start/endはJavaScript文字列のUTF-16オフセットに統一する。
- Lindera側の位置情報の単位を確認し、必要なら変換する。
- 原文を保持し、正規化でハイライト位置をずらさない。
- 形態素解析辞書と表現検出ルールを分離する。
- 辞書編集UIとユーザー辞書は後回しにする。
- WASMと辞書は可能な限りサイト自身から配信する。
- 配布物のバージョンを固定し、lockfileをコミットする。

UI:
- 白背景、落ち着いた文字色、指摘箇所に控えめな色。
- ページを開くと、大きな入力欄がすぐ見える。
- 入力欄、チェック・クリア・コピーボタン、
  文字数・指摘数、ハイライト付き結果、指摘理由を配置する。
- 初期版はtextareaと結果表示を分け、日本語入力を安定させる。
- 指摘を選ぶと入力欄の該当範囲を選択する。
- スマホは縦一列。横スクロールを発生させない。
- 入力文字は16px以上、主要ボタンの高さは44px以上。
- 色だけに頼らず、ラベルと説明で指摘を伝える。
- 辞書読み込み中、解析中、読み込み失敗、指摘なしを区別する。
- IME変換中は自動解析しない。
- 自動解析は300ms程度のデバウンスを行い、古い結果を破棄する。

本文:
- 外部API、ログ、アクセス解析、URLに本文を送らない。
- 初期版では本文を永続保存しない。
- 原文をHTMLとして解釈しない。
- 自動書き換えはせず、利用者が編集する。

最初の技術検証:
1. ブラウザ用Linderaの配布版を導入し、Workerで解析を動かす。
2. 開発サーバーと本番ビルドの両方で確認する。
3. 日本語・絵文字・改行・活用形の位置情報を確認する。
4. 配布ファイルサイズ、初回読み込み時間、初期化時間、
   1,000文字と10,000文字の解析時間を分けて測定する。
5. Pagesの1ファイル25 MiB制限を確認する。
6. 問題があれば辞書別ロード等を検討し、
   Linderaを変更する必要がある場合は根拠を説明する。

検出処理の実装時:
- 元プリセットのルールとテストを確認して移植する。
- 取り込んだコードやデータの出典・バージョンを記録する。
- 上流のライセンス表記と辞書固有の配布条件を確認して同梱する。
- 活用形、複数トークンの表現、重複した指摘、
  絵文字の後ろの位置、読み込み失敗をテストする。

デプロイ:
- Cloudflare Pages、buildはnpm run build、出力はdist。
- 本番公開前にプレビューで確認する。